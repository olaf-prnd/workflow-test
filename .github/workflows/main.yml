name: Release PR develop merge

on:
  pull_request:
    types: [ opened, synchronize, ready_for_review ]
    branches:
      - main
      
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "test_name=test_value" >> $GITHUB_OUTPUT
