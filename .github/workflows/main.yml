name: Release PR develop merge

on:
  pull_request:
    types:
      - closed
    branches:
      - main
      
jobs:
  pr_develop_merge:
    uses: PRNDcompany/prnd-android-workflows/.github/workflows/release_pr_develop_merge.yml@feature/HDA-6873-release-pr-develop-merge
