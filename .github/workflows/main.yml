name: 테스트
on:
  pull_request:
    types: [ opened, synchronize ]
  issue_comment:
    types: [ created ]

jobs:
  build:
    if: contains(github.event.comment.body, "build test")
    runs-on: ubuntu-latest
    steps:
      - run: echo test
