name: Release PR develop merge

on:
  pull_request:
    types: [ opened, synchronize, ready_for_review ]
    branches:
      - main

jobs:
  check-skip:
    uses: PRNDcompany/prnd-android-workflows/.github/workflows/check_skip.yml@main
  build:
    runs-on: ubuntu-latest
    needs: check-skip
    steps:
      - run: echo "Build Success"
