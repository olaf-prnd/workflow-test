name: 테스트
on:
  issue_comment:
    types: [ created, edited ]
jobs:
  build:
#     if: ${{ contains(github.event.comment.body, 'build test') }}
    runs-on: ubuntu-latest
    steps:
      - run: echo test
