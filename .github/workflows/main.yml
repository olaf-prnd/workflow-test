name: Release PR develop merge

on:
  pull_request:
    types:
      - closed
    branches:
      - main

jobs:
  echo_repository:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.repository }}
  pr_develop_merge:
    uses: PRNDcompany/prnd-android-workflows/.github/workflows/release_pr_develop_merge.yml@feature/HDA-9367-develop-pr-auto-merge-workflow-fix
    secrets: 
      PERSONAL_ACCESS_TOKEN: ${{ secrets.TESTER_PRND_ACCESS_TOKEN }}
